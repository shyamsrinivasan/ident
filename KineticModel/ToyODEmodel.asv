function ToyODEmodel(t,mc,data,model,pvec)

nt_m = model.nt_metab;
nin_m = model.nint_metab;
dXdt = zeros(nt_m,1);
%% Fluxes
flux = iflux(model,pvec,mc.*model.imc);
%% %Intracellular Metabolites
%Cytosolic
dXdt(1:nin_m) = (1./model.imc(1:nin_m)).*(model.S(1:nin_m,:)*flux);
dXdt(nin_m+1:nt_m) = 0;%
